# examples:
#   periodic_cleanup:
#     class: CleanSoftDeletedRecordsJob
#     queue: background
#     args: [ 1000, { batch_size: 500 } ]
#     schedule: every hour
#   periodic_cleanup_with_command:
#     command: "SoftDeletedRecord.due.delete_all"
#     priority: 2
#     schedule: at 5am every day

production:
  # LOW-031: scrub PII columns (ip_address, user_agent, lat/long,
  # face_snapshot_url) on AuditLog rows older than AUDIT_RETENTION_DAYS.
  audit_retention_cleanup:
    class: AuditRetentionCleanupJob
    queue: default
    schedule: at 3am every day

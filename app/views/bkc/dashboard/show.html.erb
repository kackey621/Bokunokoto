<% content_for :bkc_title, "Dashboard" %>

<div class="row">
  <div class="col-sm-6 col-lg-3">
    <div class="card mb-4 text-white bg-primary">
      <div class="card-body pb-0 d-flex justify-content-between align-items-start">
        <div>
          <div class="fs-4 fw-semibold"><%= @viewers_count %></div>
          <div>Total Viewers</div>
        </div>
      </div>
      <div class="c-chart-wrapper mt-3 mx-3" style="height:70px;">
      </div>
    </div>
  </div>

  <div class="col-sm-6 col-lg-3">
    <div class="card mb-4 text-white bg-info">
      <div class="card-body pb-0 d-flex justify-content-between align-items-start">
        <div>
          <div class="fs-4 fw-semibold"><%= @contents_count %></div>
          <div>Total Contents</div>
        </div>
      </div>
      <div class="c-chart-wrapper mt-3 mx-3" style="height:70px;">
      </div>
    </div>
  </div>
</div>

<div class="card mb-4">
  <div class="card-header">Vault Summary</div>
  <div class="card-body">
    <dl class="row">
      <dt class="col-sm-3">Display Name</dt>
      <dd class="col-sm-9"><%= current_vault.display_name %></dd>

      <dt class="col-sm-3">Bio</dt>
      <dd class="col-sm-9"><%= current_vault.bio.presence || "No bio set" %></dd>

      <dt class="col-sm-3">Initialized</dt>
      <dd class="col-sm-9"><%= current_vault.created_at.strftime("%Y-%m-%d") %></dd>
    </dl>
  </div>
</div>

<div class="card mb-4">
  <div class="card-header">Recent Activity</div>
  <div class="card-body p-0">
    <table class="table table-sm mb-0">
      <thead>
        <tr>
          <th>When</th>
          <th>Who</th>
          <th>Action</th>
          <th>Content</th>
        </tr>
      </thead>
      <tbody>
        <% @recent_activity.each do |log| %>
          <tr>
            <td><%= log.occurred_at.strftime("%Y-%m-%d %H:%M") %></td>
            <td><%= log.user.display_name %></td>
            <td><span class="badge text-bg-secondary"><%= log.action %></span></td>
            <td><%= log.content&.title || "—" %></td>
          </tr>
        <% end %>
        <% if @recent_activity.empty? %>
          <tr>
            <td colspan="4" class="text-center py-3 text-body-secondary">No activity yet.</td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
